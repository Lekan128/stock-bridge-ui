# Copy to .env and adjust for your local setup. Defaults to http://localhost:8080 if unset.
# See ../ENVIRONMENT.md for the full picture of every env var in the system.
VITE_API_BASE_URL=http://localhost:8080
